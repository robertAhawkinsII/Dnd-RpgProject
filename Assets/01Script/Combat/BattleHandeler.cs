using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Enemy Encounter Handler
public class BattleHandeler : MonoBehaviour
{
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
