using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerPartManager : MonoBehaviour
{

    List<GameObject> playerParty;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AddPartyMember()
    {
        //make a foor loop that will add the character in questions prefab to the list and then have the character set active to false
        //character will have a boolVariable that says inParty if true the active is set to off else active is set to true 
        //character will have to be of a child on the game object so that they will be true or not
    }
}
